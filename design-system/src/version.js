// Bump this string, rebuild ONLY the design-system, and watch both apps show
// the new value on next load — without either app being rebuilt. That is the
// "no version lock" claim, made visible.
export const VERSION = "design-system @ runtime v1.0.0";
